<div class = "container">
  <h1>GA missed connections</h1>
<ul>
  <% @posts.each do |post| %>
  <li>
    <%= post.title %> <%= post.body %> <%= post.location.campus %>
    <%= link_to "More info", post_path(post) %>
  </li>
  <% end %>
</ul>
</div>
